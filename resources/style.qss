QWizard {
    background: #ffffff;
    font-family: 'San Francisco', -apple-system, system-ui, 'Segoe UI', 'Helvetica Neue', Arial, sans-serif;
    border: none;
    padding: 0;
    margin: 0;
}
QWizardPage {
    background: transparent;
    border: none;
    padding: 0;
    margin: 0;
}
QWizard > QStackedWidget {
    border: none;
}
QWizard QFrame {
    border: none;
    background: transparent;
}

/* Sidebar styles */
#sidebar {
    background: transparent;
    padding: 20px;
}
#sidebar QLabel.step-label {
    font-size: 13px;
    color: #888888;
    font-weight: normal;
}
#sidebar QLabel.step-label[current="true"] {
    color: #333333;
    font-weight: 600;
}
#sidebar QFrame.step-circle {
    min-width: 12px;
    max-width: 12px;
    min-height: 12px;
    max-height: 12px;
    border-radius: 6px;
}
#sidebar QFrame.step-circle[current="true"] {
    background: #1976d2;
}
#sidebar QFrame.step-circle[current="false"] {
    border: 1px solid #cccccc;
    background: transparent;
}
#sidebar QFrame.step-connector {
    background: #dddddd;
    max-width: 1px;
    min-width: 1px;
}
#card-panel, .card-panel, QFrame#card-panel {
    background: transparent;
    border: none;
    box-shadow: none;
    padding: 0;
}

/* Stepper styles */
#stepper {
    margin-bottom: 16px;
    min-height: 24px;
}
#stepper QLabel {
    background: transparent;
    margin: 0 4px;
}

/* Drop zone styles */
#drop-zone {
    background: #F9F9F9;
    border: 1px dashed #CCCCCC;
    border-radius: 8px;
    transition: background 0.2s, border-color 0.2s;
}
#drop-zone[drag="true"], #drop-zone:hover, #drop-zone:focus {
    background: #FFFFFF;
    border-color: #888888;
}
#drop-zone QLabel {
    background: transparent;
    qproperty-alignment: AlignCenter;
}

/* Browse button */
QPushButton#browse-btn {
    background: #0a84ff;
    color: #fff;
    border: none;
    border-radius: 6px;
    font-size: 13px;
    font-weight: 600;
    min-width: 120px;
    max-width: 120px;
    min-height: 32px;
    max-height: 32px;
    padding: 4px 12px;
}
QPushButton#browse-btn:disabled {
    background: #c0c0c0;
    color: #fff;
}

/* File name + clear icon */
QLabel#file-name-label {
    font-size: 15px;
    color: #222;
    padding-left: 6px;
}
QPushButton#clear-btn {
    background: transparent;
    font-size: 22px;
    color: #d32f2f;
    border: none;
    margin-left: 8px;
    margin-right: 0;
    padding: 0;
}
QPushButton#clear-btn:hover {
    color: #b71c1c;
}

/* Error label */
#error-label {
    color: #d32f2f;
    font-size: 12px;
    margin-top: 0px;
    margin-bottom: 0px;
}

/* Action buttons row */
QPushButton#continue-btn, QPushButton#exit-btn, QPushButton#back-btn {
    min-width: 100px;
    max-width: 100px;
    min-height: 32px;
    max-height: 32px;
    font-size: 13px;
    font-weight: 600;
    border-radius: 6px;
    border: none;
}
QPushButton#exit-btn {
    background: #F2F2F2;
    color: #333333;
    padding: 4px 16px;
}
QPushButton#continue-btn {
    background: #c0c0c0;
    color: #ffffff;
}
QPushButton#exit-btn:disabled, QPushButton#continue-btn:disabled, QPushButton#back-btn:disabled {
    background: #e0e0e0;
    color: #A0A3A6;
}
QPushButton#exit-btn:hover {
    background: #DDDDDD;
}
QPushButton#continue-btn:hover {
    background: #0a84ff;
    color: #ffffff;
}

/* 8px grid for vertical spacing */
#card-panel > * {
    margin-top: 8px;
    margin-bottom: 8px;
}

/* Helper and error labels */
#error-label {
    color: #d32f2f;
    font-size: 12px;
    margin-top: 0px;
    margin-bottom: 0px;
}
#helper-label {
    color: #666;
    font-size: 12px;
    margin-top: 0px;
    margin-bottom: 0px;
}

/* Token input show/hide icon */
QLineEdit {
    background: #fff;
    border: 2px solid #1976d2;
    border-radius: 10px;
    padding: 10px 38px 10px 14px; /* leave space for icon */
    font-size: 16px;
    color: #222;
}
QLineEdit:disabled {
    background: #f5f5f5;
    color: #bbb;
}

QCheckBox, QCheckBox QLabel {
    color: #222;
    font-size: 15px;
}

QPushButton {
    background: qlineargradient(x1:0, y1:0, x2:1, y2:0, stop:0 #2196f3, stop:1 #64b5f6);
    color: #fff;
    border: none;
    border-radius: 10px;
    padding: 10px 24px;
    font-size: 16px;
    font-weight: 500;
    margin-top: 8px;
    margin-bottom: 8px;
}
QPushButton:disabled {
    background: #e3eaf2;
    color: #A0A3A6;
}

/* Helper link */
QLabel[objectName="helper-link"] {
    color: #1976d2;
    text-decoration: none;
    font-size: 14px;
    margin-left: 8px;
}

QLabel {
    color: #222;
    font-size: 15px;
}
QLabel[role='title'] {
    font-size: 22px;
    font-weight: bold;
    color: #1976d2;
    margin-bottom: 16px;
}

QTableWidget {
    background: #fff;
    border-radius: 10px;
    border: 1px solid #bbdefb;
    font-size: 15px;
    gridline-color: #e3f2fd;
}
QHeaderView::section {
    background: #e3f2fd;
    color: #1976d2;
    font-weight: bold;
    border: none;
    border-radius: 8px;
}
QTableWidget::item:selected {
    background: #bbdefb;
    color: #222;
}
QTableWidget QTableCornerButton::section {
    background: #e3f2fd;
    border-radius: 8px;
}
QSvgWidget {
    background: transparent;
}
.error-label {
    color: #d32f2f;
    font-weight: bold;
    font-size: 15px;
    border-radius: 8px;
    background: rgba(244, 67, 54, 0.06);
    padding: 8px 12px;
    margin-bottom: 8px;
    animation: fadeIn 0.7s;
}
.success-label {
    color: #388e3c;
    font-weight: bold;
    font-size: 15px;
    border-radius: 8px;
    background: rgba(56, 142, 60, 0.06);
    padding: 8px 12px;
    margin-bottom: 8px;
    animation: fadeIn 0.7s;
}
@keyframes fadeIn {
    0% { opacity: 0; }
    100% { opacity: 1; }
}

/* AccountSelectionPage styles */
#card-panel {
    background: transparent;
    border: none;
    padding: 0;
}

#stepper {
    margin-bottom: 16px;
    min-height: 24px;
}

/* Title */
QLabel {
    font-family: -apple-system, system-ui, 'Segoe UI', 'Helvetica Neue', Arial, sans-serif;
}
QLabel[style*="font-size:20px"] {
    font-size: 20px;
    font-weight: bold;
    color: #222;
    margin-bottom: 24px;
}

/* Labels */
QLabel[style*="font-size:14px"] {
    font-size: 14px;
    font-weight: 500;
    color: #333;
}

/* Dropdowns */
QComboBox#budget-combo, QComboBox#account-combo {
    min-height: 40px;
    font-size: 15px;
    border: 1px solid #ccc;
    border-radius: 6px;
    color: #222;
    background: #fff;
    padding: 6px 32px 6px 12px;
    width: 60%;
    min-width: 240px;
    max-width: 360px;
}
QComboBox#budget-combo QAbstractItemView, QComboBox#account-combo QAbstractItemView {
    font-size: 15px;
}
QComboBox#budget-combo::placeholder, QComboBox#account-combo::placeholder {
    color: #999;
}

/* Helper label */
QLabel#helper-label {
    font-size: 12px;
    color: #666;
    margin-bottom: 0;
    margin-top: 0;
}

/* Error label */
QLabel#error-label {
    font-size: 12px;
    color: #d32f2f;
    margin-bottom: 0;
    margin-top: 0;
}

/* Buttons */
QPushButton#back-btn {
    background: #F7F8FA;
    color: #222;
    border: 1px solid #E1E3E5;
    font-weight: 500;
    border-radius: 10px;
    padding: 10px 24px;
    font-size: 16px;
    min-width: 100px;
    min-height: 40px;
}
QPushButton#continue-btn {
    background: #1976d2;
    color: #fff;
    font-weight: 600;
    border-radius: 10px;
    padding: 10px 24px;
    font-size: 16px;
    min-width: 100px;
    min-height: 40px;
    opacity: 1;
    border: none;
    box-shadow: 0 2px 4px rgba(25, 118, 210, 0.08);
}
QPushButton#continue-btn:disabled {
    background: #e0e0e0;
    color: #A0A3A6;
    opacity: 0.6;
    cursor: not-allowed;
}

/* 8px grid for vertical spacing */
#card-panel > * {
    margin-top: 8px;
    margin-bottom: 8px;
}

/* Transaction Table Specific Styles */
#transactions-table {
    /* inherits border, background from QTableWidget */
    gridline-color: #e3f2fd; /* Ensure grid lines match the light theme */
    alternate-background-color: #ffffff; /* Explicitly set for :alternate selector */
    background-color: #f7fbff; /* Ensure default background matches */
}

#transactions-table::item {
    padding: 5px 8px; /* Add padding */
    color: #111 !important; /* Darker text color, try !important */
    background-color: transparent; /* Ensure item background is transparent by default */
    border-bottom: 1px solid #e3f2fd; /* Add subtle row separator */
    border-right: none;
    border-left: none;
    border-top: none;
}
#transactions-table::item:selected {
    background-color: #bbdefb; /* Slightly darker blue background for selection */
    color: #000 !important; /* Ensure black text on selection */
}

/* Keep header style consistent, maybe add padding */
#transactions-table QHeaderView::section {
    padding: 6px 8px;
    /* inherits background: #e3f2fd; color: #1976d2; font-weight: bold; border: none; */
}

#supported-label {
    font-size: 13px;
    color: #1976d2;
    margin-top: 8px;
    margin-bottom: 0;
}

:root {
  --gray-0: #FFFFFF;
  --gray-1: #F7F8FA;
  --gray-2: #E1E3E5;
  --gray-7: #A0A3A6;
  --blue-500: #1976d2;
}

/* MacOS Step 1 overrides */
QListWidget#sidebar {
    border: none;
    font-size: 13px;
    color: #888888;
    font-family: 'San Francisco', -apple-system, system-ui;
}
QListWidget#sidebar::item { padding: 6px; }
QListWidget#sidebar::item:selected {
    background: #007aff;
    color: #ffffff;
    font-weight: bold;
}

#main-pane QLabel#main-title {
    font-size: 20px;
    font-weight: bold;
    color: #222222;
    font-family: 'San Francisco', -apple-system, system-ui;
}
#main-pane QLabel#subtext {
    font-size: 13px;
    color: #666666;
    font-family: 'San Francisco', -apple-system, system-ui;
}

#drop-widget {
    background: #F9F9F9;
    border: 1px dashed #CCCCCC;
    border-radius: 8px;
}
#drop-widget[drag="true"] {
    background: #EFF8FF;
    border-color: #888888;
}
#drop-widget QLabel {
    color: #555555;
    font-size: 14px;
}

QPushButton#browse-btn {
    min-width: 120px;
    max-width: 120px;
    min-height: 32px;
    max-height: 32px;
    font-size: 13px;
    font-weight: 600;
}

QPushButton#continue-btn {
    min-width: 100px;
    max-width: 100px;
    min-height: 32px;
    max-height: 32px;
    font-size: 13px;
    font-weight: 600;
}
QPushButton#continue-btn:enabled {
    background: #0a84ff;
    color: #ffffff;
}
QPushButton#continue-btn:disabled {
    background: #c0c0c0;
    color: #ffffff;
}

QPushButton#exit-btn:hover {
    background: #DDDDDD;
}

